/**
 * Mobile-fit patrika sheet.
 * Width matches A4 (210mm) so it still looks like a proper page;
 * height is shorter than A4 so fit-to-width shows the WHOLE page
 * on small and large phones without vertical scrolling.
 */
export const PDF_PX_W = 1240;
export const PDF_PX_H = 1416; // 210mm × 240mm
export const PDF_MM_W = 210;
export const PDF_MM_H = 240;
